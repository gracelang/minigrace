var `a variable` := "Good"
method `and a method` {
    return `a variable`
}

print(`and a method`())

var a := object {
    var `a "field"` := 1
    var `object` := 2
}
print(a.`a "field"`)
print(a.`object`)
